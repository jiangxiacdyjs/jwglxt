
注意：这里是模拟的数据，路由默认根目录views
一、未设置jump地址的路由默认地址为views下的name层级下的index.html
二、设置jump的跳转目录为jump的实际值地址，跳转的模块需要遵循一定的格式（参照views下的temp.html格式）
三、不同模块的主栏目name值不能一样，系统会根据栏目的name值判断是否是一个模块，同一name值的模块要放在一个对象里面
四、json格式每组最后一对键值后面不允许有逗号，不允许注释
五、实际应用时，请在模板文件（.html）中的 lay-url="" 和其它相关接口的 url 设置成你真实的接口地址